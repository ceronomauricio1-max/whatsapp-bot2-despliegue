# 1. Empezar con la imagen completa de Node.js v18
FROM node:18

# 2. Instalar solo las dependencias del sistema absolutamente esenciales que la librería necesita
RUN apt-get update && apt-get install -y \
    ca-certificates \
    fonts-liberation \
    libappindicator3-1 \
    libasound2 \
    libatk-bridge2.0-0 \
    libatk1.0-0 \
    libcairo2 \
    libcups2 \
    libdbus-1-3 \
    libexpat1 \
    libfontconfig1 \
    libgbm1 \
    libgcc1 \
    libgdk-pixbuf2.0-0 \
    libglib2.0-0 \
    libgtk-3-0 \
    libnspr4 \
    libnss3 \
    libpango-1.0-0 \
    libpangocairo-1.0-0 \
    libstdc++6 \
    libx11-6 \
    libx11-xcb1 \
    libxcb1 \
    libxcomposite1 \
    libxcursor1 \
    libxdamage1 \
    libxext6 \
    libxfixes3 \
    libxi6 \
    libxrandr2 \
    libxrender1 \
    libxss1 \
    libxtst6 \
    lsb-release \
    wget \
    xdg-utils \
    --no-install-recommends \
    && rm -rf /var/lib/apt/lists/*

# 3. Preparar la carpeta para el código de nuestro bot
WORKDIR /app

# 4. Copiar los archivos de dependencias e instalarlas
# Esto descargará la versión de Chromium que viene con puppeteer
COPY package*.json ./
RUN npm install

# 5. Copiar el resto del código del bot
COPY . .

# 6. Definir el comando para iniciar el bot
CMD [ "npm", "start" ]